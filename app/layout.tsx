import type { Metadata } from 'next'
import { Inter } from 'next/font/google'
import './globals.css'

const inter = Inter({ subsets: ['latin'] })

export const metadata: Metadata = {
  title: 'PreRender SaaS - SEO-Friendly Snapshots for Your Website',
  description: 'Serve pre-rendered, SEO-friendly HTML snapshots to search engines and bots while delivering dynamic content to users.',
}

export default function RootLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return (
    <html lang="en">
      <body className={inter.className}>{children}</body>
    </html>
  )
}